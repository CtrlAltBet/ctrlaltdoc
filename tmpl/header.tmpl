<?js
    var self = this;
    var data = obj;
    var meta = data.meta;
?>
<header class="flex items-center">
    <div class="flex-grow">
        <a href="index.html"><?js= meta.title ?></a>
    </div>
<div class="flex">
<div class="header-nav">

</div>

    <?js if (data.search) { ?>
        <!-- A1 -->
        <input type="text" id="nav-search" placeholder="Search" />
        <!-- A2 -->
        <?js } ?>
        </div>
</header>
